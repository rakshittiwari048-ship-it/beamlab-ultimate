# Azure Static Web Apps Configuration
# GitHub Student Pack: 100 GB bandwidth/month FREE
# Domain: beamlabultimate.tech (via get.tech)

# This file is used by Azure Static Web Apps
# Auto-detected when you connect your GitHub repository

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================
name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          
          # Frontend configuration
          app_location: "apps/frontend"
          api_location: ""
          output_location: "dist"
          
          # Build commands
          app_build_command: "npm install && npm run build"
          
        env:
          # Build-time environment variables
          VITE_API_URL: ${{ vars.API_URL }}
          VITE_PYTHON_API_URL: ${{ vars.PYTHON_API_URL }}
          VITE_CLERK_PUBLISHABLE_KEY: ${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }}

  close_pull_request:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
